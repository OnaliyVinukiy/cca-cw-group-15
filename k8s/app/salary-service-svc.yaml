apiVersion: v1
kind: Service
metadata:
  name: salary-service
  namespace: app
spec:
  selector:
    app: salary-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP