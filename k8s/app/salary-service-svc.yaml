apiVersion: v1
kind: Service
metadata:
  name: salary_service
  namespace: app
spec:
  selector:
    app: salary_service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP