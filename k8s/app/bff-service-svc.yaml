apiVersion: v1
kind: Service
metadata:
  name: bff-service
  namespace: app
spec:
  selector:
    app: bff-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP