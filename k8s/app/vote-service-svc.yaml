apiVersion: v1
kind: Service
metadata:
  name: vote-service
  namespace: app
spec:
  selector:
    app: vote-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP